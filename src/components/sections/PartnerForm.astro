---
import FeatureIconCard from '../cards/FeatureIconCard.astro';
import Button from '../ui/Button.astro';

const features = [
  { icon: '🤝', title: 'B2B Partnership', description: 'Join our growing network of distributors' },
  { icon: '💼', title: 'Corporate Sales', description: 'Bulk orders for your business needs' },
  { icon: '🚚', title: 'Reliable Supply', description: 'Consistent quality and timely delivery' }
];
---

<section id="contact" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12">
      <div class="inline-flex items-center justify-center w-20 h-20 bg-[var(--color-kfood-primary)] rounded-full text-white text-4xl mb-4">
        🤝
      </div>
      <h2 class="text-4xl font-bold text-[var(--color-kfood-primary)] mb-2">
        Partner with Mool
      </h2>
      <p class="text-gray-600">Let's grow together</p>
    </div>
    
    <div class="grid md:grid-cols-2 gap-12 max-w-5xl mx-auto">
      <div>
        <h3 class="text-2xl font-semibold text-[var(--color-kfood-primary)] mb-6">Why Partner With Us?</h3>
        <div class="space-y-4">
          {features.map((feature) => (
            <FeatureIconCard 
              icon={feature.icon}
              title={feature.title}
              description={feature.description}
            />
          ))}
        </div>
      </div>
      
      <div class="bg-gray-50 p-8 rounded-2xl">
        <h3 class="text-xl font-semibold text-[var(--color-kfood-primary)] mb-6">Get in Touch</h3>
        <form class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Name</label>
            <input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[var(--color-kfood-orange)] focus:border-transparent outline-none" placeholder="Your name" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
            <input type="email" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[var(--color-kfood-orange)] focus:border-transparent outline-none" placeholder="your@email.com" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Phone</label>
            <input type="tel" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[var(--color-kfood-orange)] focus:border-transparent outline-none" placeholder="+92 XXX XXXXXXX" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Message</label>
            <textarea rows="4" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[var(--color-kfood-orange)] focus:border-transparent outline-none" placeholder="Tell us about your requirements"></textarea>
          </div>
          <Button variant="primary" class="w-full">
            Send Message
          </Button>
        </form>
      </div>
    </div>
  </div>
</section>
