---
const certifications = [
  { name: 'ISO 22000:2018', icon: '🏆' },
  { name: 'Organic Certified', icon: '🌱' },
  { name: 'Halal Certified', icon: '✓' },
  { name: 'Quality Assured', icon: '⭐' }
];
---

<section class="py-12 bg-white">
  <div class="container mx-auto px-4">
    <div class="flex flex-wrap justify-center items-center gap-8">
      {certifications.map((cert) => (
        <div class="flex flex-col items-center">
          <div class="w-24 h-24 bg-[var(--color-kfood-primary)] rounded-full flex items-center justify-center text-4xl text-white mb-2 shadow-lg">
            {cert.icon}
          </div>
          <span class="text-sm font-semibold text-[var(--color-kfood-primary)]">{cert.name}</span>
        </div>
      ))}
    </div>
  </div>
</section>
